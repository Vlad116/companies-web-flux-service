governmentdata.url=https://data.gov.ru/api/json/organization?access_token=41b4986d71119ab8098b47e1e903312f
#myapidata.url=
server.port=81

# Charset of HTTP requests and responses. Added to the "Content-Type" header if not set explicitly.
spring.http.encoding.charset=UTF-8
# Enable http encoding support.
spring.http.encoding.enabled=true
# Force the encoding to the configured charset on HTTP requests and responses.
spring.http.encoding.force=true

spring.datasource.username=postgres
spring.datasource.password=logistic
spring.datasource.url=jdbc:postgresql://localhost:5432/companiesdata
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
